#include<stdio.h>
int main()
{
	int arr[100],i,n,count=0;
	printf("enter no of elements:");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	for(i=0;i<n;i++)
	{
		if(arr[i]<0)
		{
			count++;
		}
	}
	printf("the no of negative numbers are %d",count);
	return 0;
}
