#include <stdio.h>
#include <ctype.h>
int main()
{     
      int p,q,r,i;
      printf("enter p");
      scanf("%d",&p);
      printf("enter q");
      scanf("%d",&q);
      printf("enter r");
      scanf("%d",&r);
      if(p>0){
      		for(i=p;i<q;i++)
      {
            if(i%10==r)
            {
                  continue;     
            }
            printf("%d ", i);
      }   
	  }
	  else
	  printf("invalid");
      return 0;
  }
